<h1>New disktest batch</h1>

<% form_for(@disktest_batch, :url => {:action => "create"}) do |f| %>
  <%= f.error_messages %>

  <%= render :partial => "form", :locals => {:f => f} %>

  <p>
    <%= f.submit "Create", :onclick => "add_all_line_items();" %>
  </p>
<% end %>

<%= link_to 'Back', {:action => "index"} %>

