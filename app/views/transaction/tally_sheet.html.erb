<center>
<h3>FREE GEEK DONATION SLIP</h3>
<b>For office use only. This is not a receipt!</b><br />
Please bring this form to the front desk for processing. Thank you for your hardware donation and any monetary contribution you are able to provide to support our work.<br />
<% seen = false %>
<% last = nil %>
<% last_r = nil %>
<table class="awesome">
  <tr>
    <td width="30">C</td>
    <td width="30">UC</td>
    <td>Description</td>
    <td>Amount</td>
  </tr>
<% @results.each do |r| %>
  <% current = r["covered"] %>
  <% current_r = r["required"] %>
  <% style = "" %>
  <% if seen and (last != current or current_r != last_r)  %>
    <% style = "border-width: 4px 1px 0 0;" %>
  <% end %>
  <tr>
    <% if current == "t" %>
      <td width="30" style="<%= style %>">&nbsp;</td>
      <td width="30" style="<%= style %>">&nbsp;</td>
    <% else %>
      <td style="<%= style %>" colspan="2">&nbsp;</td>
    <% end %>
    <td style="<%= style %>"><%= r["description"] %></td>
    <td style="<%= style %>">$<%= r["amount"] %><% if r["fee_type"] == "required" %>*<% end %></td>
  </tr>
  <% seen = true %>
  <% last = current %>
  <% last_r = current_r %>
<% end %>
</table>
<i>*Fee may be applicable for businesses not covered by Oregon E-Cycles</i><br />
FD# _________________________
</center>
