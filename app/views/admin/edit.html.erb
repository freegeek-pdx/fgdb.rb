<h1>Editing admin</h1>

<% form_for(@admin, :url => {:action => "update", :id => @admin.id}) do |f| %>
  <%= f.error_messages %>

  <%= render :partial => "form", :locals => {:f => f} %>

  <p>
    <%= f.submit "Update" %>
  </p>
<% end %>

<%= link_to 'Show', {:action => "show", :id => @admin.id} %> |
<%= link_to 'Back', {:action => "index"} %>
